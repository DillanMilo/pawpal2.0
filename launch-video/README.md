# PawPal Launch Video

Vertical Remotion launch video for PawPal, built around the app's current feature set:
pet profiles, health records, activity tracking, reminders, calendar, passport sharing, and provider discovery.

## Commands

```bash
npm install
npm run studio
npm run still
npm run render
```

The rendered MP4 is written to `out/pawpal-launch-video.mp4`.
